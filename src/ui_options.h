#pragma once

namespace idps {

// Right-click popup on the main Damage window. Renders the full settings
// list, organized into collapsible sections (default-open) so users can
// see and toggle anything without leaving the popup. Same content as the
// arc options panel, presented for fast in-game access.
void draw_popup_settings();

} // namespace idps
